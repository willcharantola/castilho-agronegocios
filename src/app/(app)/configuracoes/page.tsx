import { Settings } from "lucide-react";
import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function ConfiguracoesPage() {
  return (
    <PlaceholderScreen
      icon={Settings}
      title="Configurações"
      description="Perfil da empresa, aprovação de usuários e demais ajustes chegarão aqui em breve."
    />
  );
}
